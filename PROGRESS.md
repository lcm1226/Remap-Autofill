# Progress

## 2026-08-06

- Fixed the encrypted identity-transfer controls appearing unresponsive in version 1.2.1. Export/import progress, validation errors, and completion messages now appear directly below the transfer buttons instead of only in the distant page footer; busy buttons are temporarily disabled; clipboard writes use the declared `clipboardWrite` permission, fall back to the legacy copy command, and finally select the JSON with explicit `Ctrl+C` guidance if automatic copying is blocked. The same resilient clipboard helper now protects ordinary settings JSON copying. The AES-GCM/PBKDF2 payload format and device-local profile behavior are unchanged.
- Added a compact `Asia/Seoul` profile date on the same baseline as the masked identity summary and directly above the right-aligned delete action. New profiles show their save timestamp; edits replace it with `updatedAt`. Increased the active-profile card border and active/inactive divider to the same 2px teal emphasis. During pre-publish QA, restricted Gmail open-signal state reads and deletes to Gmail/extension UI senders and refresh operations to the options page. All 18 automated tests, JavaScript syntax checks, manifest-reference/duplicate-ID checks, secret-pattern review, and `git diff --check` pass.
- Capped only the saved identity-information list once it contains four or more profiles, leaving the form and right-column heading fixed while the profile cards scroll independently. The selected automatic-fill profile is now pinned above a divider; every other profile is displayed by `createdAt` descending, so a newly saved inactive profile appears directly below the divider. Display ordering does not mutate stored profile order or automatic-fill selection. Added core ordering coverage; all 17 tests, relevant syntax checks, and `git diff --check` pass.
- Added field-local identity validation feedback. Invalid required fields, phone numbers, dates, carriers, genders, and authentication methods now mark only their field label and input/select border in red, expose `aria-invalid`/`aria-errormessage`, focus the first invalid field on save, and clear the red state as the user corrects it. Phone and birth inputs now strip non-digits during typing or paste and enforce 11- and 8-digit maximum lengths respectively. Centralized phone validation in the identity core; all 16 tests, syntax checks, DOM-ID checks, and `git diff --check` pass.
- Removed all identity-autofill enable/automatic-mode toggles after user review. When at least one valid identity record exists, the selected record is always the sole automatic-fill source; the popup selector and options `자동 입력에 사용` action change that selection, while the manual popup button remains only as a retry. Profiles remain multi-record (maximum 20), and new records no longer silently evict an older record at the limit.
- Made identity save failures visible beside the form. The form now documents valid phone and birth formats, reports both native required-field failures and background validation messages inline, distinguishes new-save from edit-save, and explicitly states that arbitrary invalid values are rejected. Added regression coverage for two-profile storage, stable initial selection, switching the active profile, provider release of only the selected profile, and invalid dummy input. All 16 tests, syntax checks, DOM-ID checks, and `git diff --check` pass.
- Reworked the identity-autofill wording for first-time users: removed the unexplained `절차형` and `지원 화면` terms, renamed user-facing profiles to saved identity information, and aligned the options page, popup status messages, and README with the final selected-record automatic-fill model. Live Comet feedback confirmed that refreshing the options page is sufficient for these local unpacked-extension UI changes.
- After the user reloaded version 1.2.0, verified in live Chrome that Gmail still exposes exactly one open-signal button in a fresh compose window. A uniquely titled 1.2 regression draft was then selected and `Delete` again produced Gmail's Trash confirmation; the test draft was removed and the test tab was closed.
- Implemented the first clean-room Auth Autofill integration release as version 1.2.0 without copying upstream source: device-local multi-profile storage, validation/masking, fixed background access controls, popup/option controls, encrypted profile transfer, and a provider-scoped all-frame runtime for NICE CheckPlus, SCI Siren24, KCB OK-Name, and DreamSecurity Mobile-OK.
- Kept the identity runtime fill-only: it can populate identity fields and select carrier/SMS/PASS controls, but explicitly excludes legal consent, authentication requests, CAPTCHA, OTP, and final submission. Content scripts cannot read local storage directly and receive the active profile only from supported HTTPS provider origins.
- Added identity-profile, provider-classification, staged-form, unauthorized-origin, and AES-GCM encrypted-transfer tests. A test caught ambiguous `KT`/`SKT` substring matching; carrier selection now requires an exact option label or exact option value. The complete `verify-fast` suite passes 16/16.
- Browser automation is prohibited from opening `chrome://extensions`, and Windows Computer Use returned `EnumWindows failed: 지정된 경로를 찾을 수 없습니다. (0x80070003)`. The updated unpacked extension still requires one manual reload before live options/profile verification.
- Re-ran the live Gmail key-remap regression against the final open-signal code. A uniquely titled Codex test draft was selected and `Delete` produced Gmail's `대화가 휴지통으로 이동했습니다` confirmation; the test draft disappeared and no existing message was touched.
- Reviewed Auth Autofill 3.0.1 from its Chrome Web Store listing and public repository. Confirmed its profile fields, all-URL/all-frame content-script model, provider-specific flow coverage, and timer-heavy runtime; also confirmed that the repository declares no license, so direct code reuse is excluded without author permission.
- Added `AUTH_AUTOFILL_INTEGRATION_PLAN.md` with the memory assessment, clean-room boundary, device-local profile design, provider-adapter architecture, non-submit safety boundary, rollout waves, migration limits, verification matrix, and acceptance criteria. No Auth Autofill runtime code was added.
- Updated `GMAIL_OPEN_SIGNAL_PLAN.md` to reflect the completed Gmail-to-Naver real-recipient test and cleanup instead of reporting it as pending.
- Compared Mailsuite (Mailtrack), Streak, Mixmax, and HubSpot Sales using their official documentation and confirmed that the proposed Gmail open-signal architecture matches the established tracking-pixel and server-event pattern.
- Added `GMAIL_OPEN_SIGNAL_PLAN.md` with the product wording, privacy-minimizing data model, extension/service boundary, competitive limitations, implementation gates, validation matrix, and acceptance criteria.
- Attempted to resume the required new-path Chrome smoke test, but the Chrome browser-control connection was unavailable. No extension runtime or feature code was changed, and implementation remains gated on that smoke test.
- Retried browser discovery after the user confirmed Chrome was connected. The automation runtime still exposed only the Codex in-app browser. Plugin diagnostics found the Chrome executable, but the expected Chrome user-data directory was absent and the Codex native-messaging registry key was not registered.
- Attempted the requested Comet fallback check through Windows Computer Use. Both application and window discovery failed with `EnumWindows failed: 지정된 경로를 찾을 수 없습니다. (0x80070003)`, so no Comet window was controlled.
- Confirmed from the repository that settings export generates JSON in the options-page text area and supports clipboard copy; it does not create a repository file. Whether the previous extension's JSON was actually exported remains unverified until browser access is restored.
- A later clean browser-connection initialization superseded the earlier availability diagnosis: two distinct ChatGPT browser-extension instances were discovered and controlled. Their open tabs identified one as Google Chrome and the other as Comet. Gmail was reachable in both signed-in sessions.
- Probed the project extension shortcut in both browsers. The field-picker UI did not appear, so the target extension's new-path load and migrated settings are still not proven. `chrome://extensions` cannot be automated under the browser security policy; the target extension's normal options tab must be opened manually for ID and settings inspection.
- Confirmed the new-path project extension is active in Google Chrome by an end-to-end Gmail regression test. After explicit deletion approval, pressing `Delete` on a selected Codex-created empty draft triggered the configured `Shift+3` behavior; Gmail reported that the conversation moved to Trash and the draft count decreased from two to one. The older empty draft remained present and unselected.
- The user supplied the active project extension ID `mnobbickdjklamnkagccgknakjbkccnd`. Direct automation of its `chrome-extension://` options page is blocked by the browser-control security policy, so the previous extension ID and the actual JSON export/import action remain unverified rather than failed.
- Corrected the browser target after the user clarified that Government24 rules are registered in Comet, not Chrome. In Comet, the live Government24 login and simple-auth popup showed all three protected fields empty when the popup was opened after the extension's retry window, but a reload followed by an immediate popup open filled name (3 characters), birth date (8 digits), and phone suffix (8 digits). Actual personal values were never printed or submitted.
- The current Government24 simple-auth UI has an authentication-provider list and a phone-prefix select, but no SKT/KT/LGU carrier dropdown. Carrier auto-selection is therefore untestable against the current page, not a failed rule.
- Found a real delayed-DOM reliability defect: automatic autofill stopped after 12 retries (about 8.4 seconds), so fields created later were missed. Added a field-focused `MutationObserver` that reapplies matching automatic rules when new inputs, textareas, or selects are inserted, without changing Gmail key-remap behavior.
- `node --check content.js` passes for the delayed-DOM patch. The already-loaded Comet extension still reproduced the old failure after an 11-second delay, as expected for an unpacked extension that has not reloaded the changed content script; post-reload browser confirmation remains pending.
- Updated README installation wording to select the repository folder instead of naming a PC-specific absolute path.
- Began the Gmail open-signal implementation without creating external infrastructure. Added a local in-memory store and HTTP contract for opaque-token registration, transparent-GIF signals, authenticated aggregate status/deletion, expiry, and independent rate limits. The service stores hashes rather than raw token or installation credentials and has no subject, body, recipient, attachment, raw IP, or raw User-Agent fields.
- Added and passed the initial seven local automated tests for aggregation, authorization rejection, expiry, deletion, token validation, HTTP behavior, rate-limit separation, and unknown-token GIF responses.
- Implemented the Gmail open-signal extension flow: Gmail-only compose discovery, an isolated per-compose opt-in button, registration through fixed background operations, exactly one project pixel, send-button and Ctrl/Cmd+Enter detection, Gmail sent-confirmation handling, and post-send server arming so compose-time image loads are not counted.
- Added options-page controls for HTTPS service configuration, explicit limitations/data-transmission consent, enablement, aggregate status refresh, and local/server deletion. Subject labels and recipient counts stay local; the tracking API receives only opaque identifiers.
- Updated the local service with an authenticated `/arm` operation, kept pre-send pixel requests uncounted, and added a local development launcher.
- Added a mock-Chrome integration suite that exercises configuration, token registration, pre-send suppression, arming, pixel counting, refresh, deletion, and localhost/HTTPS URL validation against the real local HTTP service. All nine tests pass.
- Checked the live Korean Gmail compose DOM in Chrome. The implementation matches the current dialog, `role=textbox` contenteditable body, `input[name=subjectbox]`, and the primary `보내기 (Ctrl-Enter)` button; `보내기 옵션 더보기` is explicitly excluded.
- Updated manifest version to 1.1.0 and added Gmail-only tracking JavaScript/CSS.
- Added a public Cloudflare Worker implementation backed by SQLite Durable Objects. It retains only hashed opaque credentials/tokens, timestamps, aggregate signal counts, expiry metadata, and rate-limit windows; it does not read or store message content, subject, recipient addresses, attachments, raw IP addresses, or raw User-Agent strings.
- Kept Wrangler dependencies, cache, output, logs, and temporary authentication state inside the repository, and added a repository-scoped deployment wrapper.
- After the user approved deployment, published the Worker at `https://keyremap-gmail-open-signal.phantom-cinnamon-008.workers.dev` (version `3071ccc4-68a9-4d46-a6ba-9ec9ee2bdf88`). The claimable Cloudflare preview account still requires ownership claim within its one-hour window.
- Passed a live public-service smoke test using cryptographically random temporary data: registration, post-send arming, transparent GIF response, exactly one aggregated signal, authenticated deletion, and 404 after deletion. No test record remains.
- Set the deployed service as the extension's default endpoint while leaving tracking and data-transmission consent OFF by default. A reload of the unpacked extension and an explicitly authorized controlled send/open test remain pending.
- Re-ran the repository `verify-fast` workflow after dependency recovery: JavaScript syntax checks and all nine local tests pass. The repository-scoped Wrangler wrapper also passes a Cloudflare dry-run with both Durable Object bindings.
- The user confirmed that the temporary Cloudflare deployment was claimed. The public Worker continued to answer an HTTPS OPTIONS health check with 204; claiming ownership does not automatically authenticate the local Wrangler CLI, so future redeployment still requires a Cloudflare login or API token.
- Opened a fresh signed-in Gmail tab in Chrome and created a compose window. The compose dialog appeared, but no `.kr-open-signal-button` was present, proving that Chrome still has the pre-change unpacked extension runtime loaded. Browser automation cannot control the blocked `chrome://extensions`/`chrome-extension://` pages, so a one-time manual extension reload followed by a Gmail tab reload is the current handoff step.
- After the user manually reloaded the unpacked extension, reloaded Gmail and confirmed two compose dialogs with two visible `열람 신호` buttons. Clicking the newest compose button correctly stopped before registration because the stored feature setting is still disabled, displayed `옵션에서 Gmail 열람 신호 기능을 먼저 켜세요.`, and inserted no pixel. No email was sent; enabling consent and the feature in the extension options is the remaining activation step.
- Removed the extension-options dependency from first use: clicking `열람 신호` in Gmail now performs a fixed, Gmail-only activation using the deployed default service before registration. Existing pre-change Gmail buttons are also supported by auto-activating on `OPEN_SIGNAL_REGISTER`. The updated verification suite passes 10/10. A 35-second service-worker idle/retry proved Chrome still retains the loaded unpacked background bundle until the extension itself is reloaded, so one final manual extension reload is genuinely required; no options-page changes are needed afterward.
- After that final reload, completed the live Chrome compose verification successfully: the per-message button changed to `열람 신호 켜짐`, `aria-pressed` became true, the status changed to `전송 후 열람 신호 대기`, and exactly one tracking pixel used the deployed Worker origin. Turned the test signal back off afterward; the button returned to inactive, the pixel count returned to zero, and the unsent server test record was deleted. No email was sent.
- With explicit send approval, completed a real Gmail-to-Naver cross-provider test using a non-sensitive test subject/body. Gmail confirmed `메시지 전송됨`; the signed-in Naver inbox increased from 20 to 21 messages and received the exact subject. Opening it once showed the expected body plus one Worker-origin image with `complete=true` and natural dimensions `1×1`, confirming MIME survival and an actual recipient-side image load. The exact aggregate request count remains to be read from the extension's protected history UI; the test email and tracking record are intentionally retained until that final check.
- The user read the protected history result as exactly `열람 신호 1회` and deleted the server tracking record. Completed cleanup afterward: moved the Naver received test message to Trash (inbox 21 back to 20) and moved the Gmail sent conversation to Trash with Gmail's `대화가 휴지통으로 이동했습니다` confirmation. The deployed Gmail-to-Naver open-signal E2E is therefore fully verified and leaves no active test tracking record.

## 2026-04-18

- Prepared the project for Chrome Web Store submission by tightening manifest permissions, adding store-ready icons and listing assets, creating a privacy policy, and documenting submission copy.
- Added repeatable asset-generation and release-packaging scripts so screenshots, promo images, icons, and the upload ZIP can be rebuilt locally.
- Verified the updated JavaScript files, manifest parsing, asset generation, and release packaging flow.
